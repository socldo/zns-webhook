export enum Percent {
  MIN = 0,
  MAX = 100,
}

export enum ValueType {
  NUMBER = 0,
  STRING = 1,
}

export enum ResetPassword {
  NEW_PASS = "0000",
}

export enum ZnsCampaignStatus {
  DRAFT = 0,
  PENDING = 1,
  COMPLETED = 2,
  FAILED = 3
}

